/**
 * Sync between Chrome Storage and IndexedDB layers.
 * Phase 3+ — coordinates data between storage backends.
 */

// Currently not needed since Chrome Storage handles settings
// and IndexedDB handles vocabulary independently.
// Will be expanded if we need cross-device vocabulary sync.
